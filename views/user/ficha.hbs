<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{animal.name}} - Ficha</title>
    <link rel="stylesheet" href="/css/minimal.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <nav class="sidebar">
        <div class="logo">🐾 VetCare</div>
        <ul class="nav">
            <li><a href="/user/dashboard"><i class="fas fa-paw"></i><span>Animais</span></a></li>
            <li style="margin-top: auto;"><a href="/auth/logout"><i class="fas fa-sign-out-alt"></i><span>Sair</span></a></li>
        </ul>
    </nav>

    <div class="main">
        <div class="topbar">
            <div style="display:flex; gap: 10px; align-items:center;">
                <a href="/user/dashboard" class="btn btn-primary btn-sm">← Voltar</a>
                <h1 style="margin:0;">{{animal.name}}</h1>
                <span class="badge badge-primary">{{animal.status}}</span>
            </div>
        </div>

        <!-- Header com foto principal + info básica -->
        <div class="shell">
            <div style="display:grid; grid-template-columns: 260px 1fr; gap: 20px; margin-bottom: 20px;">
                <div>
                    <div class="animal-img" style="background-image: url('/animal/photo/{{animal.id}}'); height: 240px; margin-bottom: 15px;"></div>
                </div>
                <div>
                    <div class="card">
                        <h3>Informações Básicas</h3>
                        <div style="display:grid; gap: 12px;">
                            <div>
                                <label class="form-label">Espécie</label>
                                <p style="margin:0; font-weight:500;">{{animal.species}}</p>
                            </div>
                            <div>
                                <label class="form-label">Raça</label>
                                <p style="margin:0; font-weight:500;">{{animal.breed}}</p>
                            </div>
                            <div style="display:grid; grid-template-columns: 1fr 1fr; gap:10px;">
                                <div>
                                    <label class="form-label">Idade</label>
                                    <p style="margin:0; font-weight:500;">{{animal.age}} anos</p>
                                </div>
                                <div>
                                    <label class="form-label">Sexo</label>
                                    <p style="margin:0; font-weight:500;">{{animal.sex}}</p>
                                </div>
                            </div>
                            <div>
                                <label class="form-label">Entrada</label>
                                <p style="margin:0; font-weight:500;">{{animal.entry_date}}</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Galeria de Fotos (visualizador pode adicionar) -->
        <div class="card">
            <div style="display:flex; justify-content: space-between; align-items:center;">
                <h3><i class="fas fa-images"></i> Fotos do Animal</h3>
            </div>
            {{#if photos.length}}
            <div class="grid" style="grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));">
                {{#each photos}}
                <div class="animal-card">
                    <div class="animal-img" style="background-image: url('/animal/photo-extra/{{this.id}}'); height: 160px;"></div>
                    <div class="animal-info" style="padding: 10px;">
                        <div class="animal-name" style="font-size: 0.95rem;">{{this.description}}</div>
                        <div class="animal-tags">
                            <span class="tag">{{this.upload_date}}</span>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
            {{else}}
            <p class="empty">Nenhuma foto adicional</p>
            {{/if}}

            <form action="/user/animal/{{animal.id}}/photo" method="POST" enctype="multipart/form-data" style="margin-top: 15px; display: grid; grid-template-columns: 1fr auto; gap: 10px;">
                <input type="file" name="photo" accept="image/*" class="form-control" required>
                <input type="text" name="description" placeholder="Descrição (opcional)" class="form-control">
                <button type="submit" class="btn btn-success">Enviar Foto</button>
            </form>
        </div>

        <!-- Saúde (somente leitura) -->
        <div class="card">
            <h3><i class="fas fa-heartbeat"></i> Saúde</h3>
            {{#if healthRecord}}
            <div style="display:grid; gap: 10px;">
                <div>
                    <label class="form-label">Condição corporal</label>
                    <p style="margin:0; font-weight:500;">{{healthRecord.body_condition}}</p>
                </div>
                <div>
                    <label class="form-label">Observações</label>
                    <p style="margin:0; font-weight:500;">{{healthRecord.observations}}</p>
                </div>
                <div>
                    <label class="form-label">Alergias</label>
                    <p style="margin:0; font-weight:500;">{{healthRecord.allergies}}</p>
                </div>
            </div>
            {{else}}
            <p class="empty">Nenhum registro de saúde</p>
            {{/if}}
        </div>
    </div>
</body>
</html>

