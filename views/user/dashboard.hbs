<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Animais - VetCare</title>
    <link rel="stylesheet" href="/css/minimal.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <nav class="sidebar">
        <div class="logo">🐾 VetCare</div>
        <ul class="nav">
            <li><a href="/user/dashboard" class="active"><i class="fas fa-paw"></i><span>Animais</span></a></li>
            <li style="margin-top: auto;"><a href="/auth/logout"><i class="fas fa-sign-out-alt"></i><span>Sair</span></a></li>
        </ul>
    </nav>

    <div class="main">
        <div class="topbar">
            <h1>Todos os Animais</h1>
        </div>

        {{#if animals.length}}
        <div class="grid">
            {{#each animals}}
            <div class="animal-card">
                <div class="animal-img" {{#if this.photo}}style="background-image: url('/animal/photo/{{this.id}}')"{{/if}}></div>
                <div class="animal-info">
                    <div class="animal-name">{{this.name}}</div>
                    <div class="animal-tags">
                        <span class="tag">{{this.species}}</span>
                        <span class="tag">{{this.sex}}</span>
                        <span class="tag">{{this.age}} anos</span>
                    </div>
                    <a href="/user/animal/{{this.id}}" class="btn btn-primary btn-sm w-full">Ver Detalhes</a>
                </div>
            </div>
            {{/each}}
        </div>
        {{else}}
        <div class="card empty">
            <div style="font-size: 3rem; margin-bottom: 10px;">📭</div>
            <p>Nenhum animal disponível</p>
        </div>
        {{/if}}
    </div>
</body>
</html>
