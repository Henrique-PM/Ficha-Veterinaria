<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - VetCare</title>
    <link rel="stylesheet" href="/css/minimal.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <nav class="sidebar">
        <div class="logo">🐾 VetCare</div>
        <ul class="nav">
            <li><a href="/vet/dashboard"><i class="fas fa-home"></i><span>Dashboard</span></a></li>
            <li><a href="/vet/biblioteca" class="active"><i class="fas fa-image"></i><span>Biblioteca</span></a></li>
            <li><a href="/vet/consultas"><i class="fas fa-stethoscope"></i><span>Consultas</span></a></li>
            <li><a href="/vet/medicamentos"><i class="fas fa-pills"></i><span>Medicamentos</span></a></li>
            <li><a href="/vet/relatorios"><i class="fas fa-chart-line"></i><span>Relatórios</span></a></li>
            <li style="margin-top: auto;"><a href="/auth/logout"><i class="fas fa-sign-out-alt"></i><span>Sair</span></a></li>
        </ul>
    </nav>

    <div class="main">
        <div class="topbar">
            <h1>{{title}}</h1>
        </div>

        <div class="card">
            <h2>Filtrar Animais</h2>
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 15px;">
                <div class="form-group">
                    <label class="form-label">Espécie</label>
                    <select id="filter-species" class="form-control">
                        <option value="">Todas</option>
                        <option value="Cachorro">Cachorro</option>
                        <option value="Gato">Gato</option>
                        <option value="Outros">Outros</option>
                    </select>
                </div>
                <div class="form-group">
                    <label class="form-label">Status</label>
                    <select id="filter-status" class="form-control">
                        <option value="">Todos</option>
                        <option value="shelter">Abrigo</option>
                        <option value="adopted">Adotado</option>
                        <option value="treatment">Tratamento</option>
                    </select>
                </div>
                <div class="form-group">
                    <label class="form-label">&nbsp;</label>
                    <button onclick="filterPhotos()" class="btn btn-primary w-full">
                        <i class="fas fa-filter"></i> Filtrar
                    </button>
                </div>
            </div>
        </div>

        <div class="grid">
            {{#each animals}}
            <div class="animal-card">
                <div class="animal-img" style="background-image: url('/animal/photo/{{this.id}}')">
                    <div style="position: absolute; top: 10px; right: 10px;">
                        <span class="badge badge-primary">{{this.status}}</span>
                    </div>
                </div>
                <div class="animal-info">
                    <div class="animal-name">{{this.name}}</div>
                    <div class="animal-tags">
                        <span class="tag"><i class="fas fa-dog"></i> {{this.species}}</span>
                        <span class="tag"><i class="fas fa-birthday-cake"></i> {{this.age}} anos</span>
                    </div>
                    <p style="font-size: 0.9rem; color: var(--muted); margin-bottom: 15px;">{{this.breed}}</p>
                    <div style="display: flex; gap: 8px;">
                        <a href="/vet/animal/{{this.id}}" class="btn btn-primary btn-sm" style="flex: 1;">
                            <i class="fas fa-file-alt"></i> Ficha
                        </a>
                        <button onclick="expandPhoto({{this.id}}, '{{this.name}}')" class="btn btn-success btn-sm" style="flex: 1;">
                            <i class="fas fa-expand"></i> Ampliar
                        </button>
                    </div>
                </div>
            </div>
            {{else}}
            <div class="empty" style="grid-column: 1 / -1;">
                <div style="font-size: 3rem; margin-bottom: 10px;">📭</div>
                <p>Nenhum animal encontrado</p>
            </div>
            {{/each}}
        </div>
    </div>

    <!-- Modal para Visualização Ampliada -->
    <div id="photoModal" class="modal">
        <div class="modal-content" style="max-width: 700px; width: 90%; margin: 50px auto;">
            <div class="modal-header">
                <h2 id="modalAnimalName"></h2>
                <button class="close" onclick="closePhotoModal()">&times;</button>
            </div>
            <div style="padding: 20px; text-align: center;">
                <img id="modalPhoto" src="" style="max-width: 100%; border-radius: 8px; max-height: 60vh;">
            </div>
        </div>
    </div>

    <script>
        function expandPhoto(id, name) {
            document.getElementById('modalAnimalName').textContent = name;
            document.getElementById('modalPhoto').src = '/animal/photo/' + id;
            document.getElementById('photoModal').style.display = 'block';
        }

        function closePhotoModal() {
            document.getElementById('photoModal').style.display = 'none';
        }

        function filterPhotos() {
            const species = document.getElementById('filter-species').value;
            const status = document.getElementById('filter-status').value;
            console.log('Filtrar por:', species, status);
            // Implementar lógica de filtro se necessário
        }

        window.onclick = function(e) {
            if (e.target.id === 'photoModal') closePhotoModal();
        }
    </script>
</body>
</html>
